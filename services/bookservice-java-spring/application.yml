spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rxlog
    username: rxlog
    password: rxlog
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration